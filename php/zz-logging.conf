[global]
; Redirect error log to stderr in a way that works after privilege drop via su-exec.
; /proc/self/fd/2 fails when the process owner changes; /dev/stderr is resolved
; at open() time and works correctly for the child process.
error_log = /dev/stderr
